[package]
name = "governance"
version = "0.1.0"
edition = "2026"

[dependencies]
soroban-sdk = "0.9.0"
security = { path = "../security" }  # Dependency on security contract

[lib]
crate-type = ["cdylib"]
